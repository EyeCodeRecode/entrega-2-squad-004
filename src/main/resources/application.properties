# Configurações para MySQL
#spring.datasource.url=jdbc<:mysql:1128038384997249065>//localhost:3306/eyecode?createDatabaseIfNotExist=true&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=2707
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Configurações para PostgreSQL (com placeholders para variáveis de ambiente)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.sql.init.mode=embedded
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true

# Configuração de depuração (debug)
debug=true